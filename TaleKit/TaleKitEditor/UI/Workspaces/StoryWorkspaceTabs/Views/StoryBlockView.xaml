<UserControl x:Class="TaleKitEditor.UI.Workspaces.StoryWorkspaceTabs.Views.StoryBlockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:VectorImages="clr-namespace:TaleKitEditor.Resources.VectorImages"

             mc:Ignorable="d" 
             d:DesignWidth="220">
	<UserControl.Resources>
		<SolidColorBrush x:Key="SeparatorColor" Color="#FF686868"/>
	</UserControl.Resources>
	<Grid ClipToBounds="True" Height="40">
		<DockPanel Name="ItemPanel" Background="{StaticResource ItemBackground}">
			<VectorImages:VisibleIcon x:Name="VisibleButton" VerticalAlignment="Center" Margin="4,0,0,0"/>
			<Grid>
				<Grid Name="ContentContext">


				</Grid>
			</Grid>
		</DockPanel>

		<!-- Arrow visual -->
		<Border Background="{StaticResource SeparatorColor}" Height="2" VerticalAlignment="Top"/>
		<Polygon Stroke="{StaticResource SeparatorColor}" StrokeThickness="2" Fill="{StaticResource ItemBackground}" Points="0,0, 16,0, 8,8" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-1,0,0"/>
	</Grid>
</UserControl>
