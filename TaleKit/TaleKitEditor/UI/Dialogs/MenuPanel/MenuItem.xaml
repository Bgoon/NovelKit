<UserControl x:Class="TaleKitEditor.UI.Dialogs.MenuItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
	<Grid Background="{StaticResource TabBackground}">
		<TextBlock Style="{StaticResource ContentText}" Padding="8" Text="{Binding ItemName, Mode=OneWay, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}}"></TextBlock>
		<Border Name="ButtonPanel" Background="Transparent"/>
	</Grid>
</UserControl>
